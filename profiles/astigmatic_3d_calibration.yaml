extends: liteloc_unified_example.yaml

profile_name: astigmatic_3d_calibration

experiment:
  psf_type: astigmatic
  dimensionality: 3d

microscope:
  pixel_size_nm: 100

calibration:
  enabled: true
  mode: vector_beads
  z_step_nm: 50
